#include <stdio.h>

/**
 * main - this is to create and output strings formatted at runtime
 *
 * Return: 0 when successful
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);

}
