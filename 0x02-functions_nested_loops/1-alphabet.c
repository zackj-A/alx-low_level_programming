#include "main.h"

/**
 * print_alphabet - function is to print alphabet in lower case
 * Return: returns 0
 */

void print_alphabet(void)
{
	char i;

	for (i = 'a' ; i <= 'z' ; i++)

		_putchar(i);
	_putchar('\n');
}
